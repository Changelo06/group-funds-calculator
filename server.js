const http=require("http"),fs=require("fs"),path=require("path"),api=require("./api"),root=__dirname;
const types={".html":"text/html; charset=utf-8",".css":"text/css; charset=utf-8",".js":"text/javascript; charset=utf-8",".png":"image/png",".json":"application/json; charset=utf-8"};
function serveStatic(request,response){const url=new URL(request.url,"http://localhost"),relative=decodeURIComponent(url.pathname==="/"?"/index.html":url.pathname),file=path.resolve(root,`.${relative}`);if(!file.startsWith(root+path.sep)){response.writeHead(403);return response.end("Forbidden");}fs.readFile(file,(error,data)=>{if(error){response.writeHead(404);return response.end("Not found");}response.writeHead(200,{"Content-Type":types[path.extname(file)]||"application/octet-stream"});response.end(data);});}
http.createServer((request,response)=>request.url.startsWith("/api/")?api(request,response):serveStatic(request,response)).listen(Number(process.env.PORT||4173),()=>console.log("Splitwise House running at http://127.0.0.1:4173"));
